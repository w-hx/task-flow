# 流时 (TaskFlow) V1.0 用户使用手册

## 1. 引言

### 1.1 编写目的
本手册旨在为“流时 (TaskFlow)”软件的用户提供详尽的操作指南与功能说明，帮助用户快速上手并充分利用该软件进行时间管理。

### 1.2 软件背景
在现代快节奏的工作与学习环境中，高效的时间管理变得尤为重要。“流时”是一款基于 Claymorphism（粘土拟态）设计风格的轻量级桌面时间管理应用。它通过简洁直观的时间表编辑功能、优雅的视觉交互以及非侵入式的状态栏提醒，帮助用户保持专注，掌控时间流逝。

### 1.3 适用对象
本手册适用于所有希望提高时间利用效率的个人用户，包括学生、职场人士、自由职业者等。

---

## 2. 软件概述

### 2.1 软件简介
“流时”是一款专注于任务流管理的时间表工具。不同于传统的番茄钟或待办清单，它强调“全天候的时间流”概念。用户可以预设一整天或特定时段的时间表（例如“早晨例程”、“深度工作时段”），软件将自动跟踪当前时间对应的任务，并在状态栏实时显示剩余时间与进度，配合悦耳的提示音，让时间管理变得轻松且具有仪式感。

### 2.2 核心功能
1.  **多时间表管理**：支持创建、保存和切换多个不同的时间表（如：工作日、周末、学习计划）。
2.  **文本化编辑**：采用极简的文本编辑方式（HH:MM-HH:MM 任务名），快速录入和修改计划。
3.  **实时状态栏反馈**：在电脑状态栏（Tray）实时显示当前任务名称、倒计时及进度，无需打开主窗口即可掌握进度。
4.  **智能提示音**：支持自定义任务开始和结束的提示音，内置多种风格（如：Magic, Chime, Electronic 等）。
5.  **Claymorphism 视觉风格**：采用柔和的 3D 粘土拟态设计，提供舒适、解压的视觉体验。

### 2.3 运行环境
-   **操作系统**：macOS 10.13 及以上版本
-   **硬件要求**：
    -   处理器：Intel 或 Apple Silicon (M1/M2/M3)
    -   内存：4GB 及以上
    -   硬盘空间：200MB 可用空间

---

## 3. 安装与启动

### 3.1 安装步骤
1.  下载最新的 `TaskFlow-x.x.x.dmg` 安装包。
2.  双击打开 `.dmg` 文件。
3.  将“流时”图标拖入 `Applications`（应用程序）文件夹。
4.  安装完成。

### 3.2 启动软件
1.  在“应用程序”文件夹或“启动台”中找到“流时”图标。
2.  点击图标启动。
3.  首次启动时，软件会显示主界面，并在顶部状态栏显示一个小图标。

---

## 4. 功能操作指南

### 4.1 主界面介绍
软件主界面采用左右分栏设计：
-   **左侧边栏（时间表列表）**：显示所有已保存的时间表卡片。
    -   **新建按钮 (+)**：点击底部的圆形加号按钮，创建一个新的空白时间表。
    -   **删除模式 (-)**：点击顶部的减号按钮，进入删除模式，点击卡片即可删除；再次点击（变为返回图标）退出删除模式。
    -   **运行状态**：正在运行的时间表会显示一个绿色的状态指示点。
-   **右侧主区域（编辑与设置区）**：
    -   **文本编辑器**：用于输入时间安排。格式为：`开始时间-结束时间 任务内容`（例如：`09:00-09:45 阅读`）。
    -   **提示音设置**：底部面板提供“开始提示音”和“结束提示音”的下拉选择框，支持点击预览声音。
    -   **操作按钮**：
        -   **保存**：保存当前编辑的内容。
        -   **开始运行 / 停止运行**：启动或停止当前时间表的计时服务。

### 4.2 创建新时间表
1.  在左侧边栏底部，点击圆形的 **“+”** 按钮。
2.  列表底部会出现一个新的卡片，默认命名为“未命名”。
3.  在右侧编辑区，输入您的计划内容。
4.  点击右下角的 **“保存”** 按钮。
    *   *注意：系统会自动提取第一行任务的内容或根据时间生成默认标题。*

### 4.3 编辑时间表
1.  **输入格式**：
    请严格按照 `HH:MM-HH:MM 任务名称` 的格式输入，每行一个任务。
    *示例：*
    ```text
    08:00-08:30 晨间阅读
    08:30-09:00 早餐与休息
    09:00-11:30 深度工作
    ```
2.  **自动校验**：
    软件会自动检查时间格式是否正确、时间段是否重叠。如果格式有误，保存时会弹出错误提示。
3.  **保存修改**：
    编辑完成后，务必点击 **“保存”** 按钮，否则更改不会生效。

### 4.4 设置提示音
在右侧编辑区的底部，有两个下拉菜单：
-   **开始提示音**：任务开始时播放的声音（如：Magic）。
-   **结束提示音**：任务结束时播放的声音（如：Electronic）。
-   **试听**：选择某个声音后，软件会自动播放一次预览。
-   **静音**：如果不需要提示音，可选择“无”或类似选项（视具体版本而定，默认提供多种音效）。

### 4.5 运行时间表
1.  选中一个时间表。
2.  点击右下角的 **“开始运行”** 按钮。
3.  **状态变化**：
    -   左侧列表中，该时间表卡片会出现绿色圆点标记。
    -   按钮变为红色的 **“停止运行”**。
    -   电脑顶部状态栏（Tray）会出现当前正在进行的任务名称和倒计时。

### 4.6 状态栏与托盘交互
当时间表开始运行后，软件的核心交互转移至状态栏：
1.  **实时显示**：状态栏图标旁会滚动显示当前任务名称（如“深度工作...”）以及剩余时间/总时间（如 `24:00 / 45:00`）。
2.  **托盘菜单**：
    点击状态栏图标，会弹出菜单：
    -   **下一个任务**：预览即将开始的任务（灰色显示，仅供查看）。
    -   **主面板**：点击可快速呼出软件主界面。
    -   **退出**：彻底退出软件（此时会停止所有计时）。
3.  **自动切换**：
    当前任务结束后，软件会自动切换到下一个时间段的任务，并播放结束提示音和下一任务的开始提示音。

### 4.7 删除时间表
1.  在左侧边栏顶部，点击圆形的 **“-”**（减号）按钮。
2.  界面进入“删除模式”，按钮变为黄色的返回图标，时间表卡片上会出现删除提示（或视觉变化）。
3.  点击您想要删除的时间表卡片。
4.  删除完成后，再次点击顶部的返回按钮，退出删除模式。

---

## 5. 常见问题与故障排除

### 5.1 无法保存时间表？
-   **原因**：通常是因为输入格式不正确或时间段有重叠。
-   **解决**：请检查每一行是否严格遵循 `HH:MM-HH:MM` 格式，且确保结束时间晚于开始时间，不同任务的时间段不能重叠。

### 5.2 状态栏没有显示任务？
-   **原因**：当前时间不在任何设定的任务时间段内。
-   **解决**：请检查您的时间表，确保包含当前时间段的任务。如果当前是空闲时间，状态栏将显示“无任务”或仅显示图标。

### 5.3 提示音没有响？
-   **原因**：系统静音、软件音量设置或“专注模式”拦截。
-   **解决**：
    1.  检查 Mac 系统音量是否开启。
    2.  确认在软件设置中选择了有效的提示音（非静音）。
    3.  检查系统通知设置，确保允许应用发出声音。

### 5.4 如何开机自启？
-   **说明**：当前版本暂未内置一键开机自启选项。
-   **解决**：您可以在 macOS 的“系统设置” -> “通用” -> “登录项”中，手动添加“流时”应用。

---

## 6. 技术支持
如果您在使用过程中遇到任何无法解决的问题，或有功能建议，请联系开发团队。

-   **联系邮箱**：support@taskflow.app
-   **官方网站**：www.taskflow.app

*(本手册内容仅供参考，具体功能以软件实际版本为准)*
